{
  "name": "ayu-plus",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "dev": "vite --port 8088 --open",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}